services:
  - type: web
    name: thepulse
    runtime: python
    buildCommand: "pip install -r requirements.txt"
    startCommand: "gunicorn --bind=0.0.0.0:$PORT --timeout 600 app:app"
    envVars:
      - key: PYTHON_VERSION
        value: "3.11.6"
